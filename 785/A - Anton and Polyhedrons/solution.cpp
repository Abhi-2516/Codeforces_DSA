#include <bits/stdc++.h>using namespace std;int main() {    int n;    cin >> n;    map<string, int> count;        for (int i = 0; i < n; i++) {        string s;        cin >> s;        count[s]++;    }        int total = 0;    total += count["Tetrahedron"] * 4;    total += count["Cube"] * 6;    total += count["Octahedron"] * 8;    total += count["Dodecahedron"] * 12;    total += count["Icosahedron"] * 20;        cout << total;    return 0;}